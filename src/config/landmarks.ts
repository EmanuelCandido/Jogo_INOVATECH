import type {Placement} from '../game/types';
// Shared with the narrative route, without importing the generated city/forest.
export const hospitalPlacement:Placement={asset:'building.hospital',position:[-15,.09,8.5],scale:[1.48,1.2,1.2],rotation:[0,0,0]};
